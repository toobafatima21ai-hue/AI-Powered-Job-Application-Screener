# AI-Powered-Job-Application-Screener
An agentic Python pipeline that reads job requirements and candidate profiles, evaluates each candidate against the role using an LLM (Google Gemini), and produces a structured JSON report with a `Shortlist` / `Reject` / `Maybe` verdict, a 0–100 match score, and a human-readable reason per candidate.
